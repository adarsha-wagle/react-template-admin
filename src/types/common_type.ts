export interface IContactForm {
  id: string;
  name: string;
  email: string;
  phoneNumber: string;
  message: string;
  status: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface IBooking {
  id: string;
  name: string;
  email: string;
}
